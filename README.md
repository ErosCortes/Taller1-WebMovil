# Taller1-WebMovil

El registros.html ahora tiene para guardar de forma local los registros
y hace que se muestren en el dashboard, al igual que los archivos del json. Sin embargo para que los archivos del json puedan leerse, tienen que crear un servidor local con del node.js o con live server de VS, de esa forma carga los datos del .json
Eso es para live server:
python -m http.server